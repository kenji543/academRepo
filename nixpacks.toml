providers = ["node", "python"]

[phases.build]
cmds = ["npm run build", "python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"

# Security: Secrets should be provided at runtime via environment variables,
# not passed as build args. Configure secrets in Railway dashboard.
